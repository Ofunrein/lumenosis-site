# TikTok Login Kit review prototype

Remotion source for a silent, 30-second Sandbox walkthrough. It is a visual prototype, not a substitute for a real OAuth recording. Before TikTok review, replace it with a capture of the live Sandbox consent flow.

```bash
pnpm typecheck
pnpm render
```
